

<script>
console.log("Hi Dan")
$( "#logo" ).click(function() {
  alert( "Handler for .click() called." );
});
</script>

<!-- Page Content -->
    <div class="container">

        <!-- Page Heading -->
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">Recruiter Agencies
                    <!-- <small>Secondary Text</small> -->
                </h1>
            </div>
        </div>
        <!-- /.row -->

        <!-- Project One -->
  <% @agencies.each do |a| %>
        <div class="row">

        <div class="col-md-12 agency-box">
            <a href="/agencies/<%=a.id %>"> <img src="/logos/<%= a.logo %>" alt="logo"/></a>
              <h3><%= a.name %></h3>
              <a href="<%= a.website %>">vitamintalent.com</a>
                <h3><%= a.reviews.average(:rating).round %><i class="fa fa-star"></i></h3>
                  <h4><%= a.about %></h4>
                <a href="/agencies/<%=a.id %>" class="btn btn-primary btn-small active" role="button">+ Review</a>
                <br><p>HQ: <%= a.headquarters %></p>
             <hr>
        

        </div>
        <!-- /.row -->
    </div>
        <hr>
   <% end %>
